name: test
on:
  push:
    branches:
      - main
      # - 'feature/*'
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      # - name: Test with docker-compose stack
      #   run: |
      #     docker-compose up -d --remove-orphans
      #     docker exec app go test -cover -v ./...
      - name: ls
        run: ls
      - name: pwd
        run: pwd
      - name: echo
        run: echo ${{ secrets.HOGE_TOKEN }}
      - shell: bash
        env:
          SUPER_SECRET: ${{ secrets.HOGE_TOKEN }}
        run:
          export LOG_FILE_PATH="$SUPER_SECRET"
          echo $LOG_FILE_PATH
          
          